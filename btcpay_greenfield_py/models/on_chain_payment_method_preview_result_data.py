from __future__ import annotations

from collections.abc import Mapping
from typing import TYPE_CHECKING, Any, TypeVar

from attrs import define as _attrs_define

from ..types import UNSET, Unset

if TYPE_CHECKING:
    from ..models.on_chain_payment_method_preview_result_address_item import (
        OnChainPaymentMethodPreviewResultAddressItem,
    )


T = TypeVar("T", bound="OnChainPaymentMethodPreviewResultData")


@_attrs_define
class OnChainPaymentMethodPreviewResultData:
    """
    Attributes:
        addresses (list[OnChainPaymentMethodPreviewResultAddressItem] | Unset): a list of addresses generated by the
            derivation scheme
    """

    addresses: list[OnChainPaymentMethodPreviewResultAddressItem] | Unset = UNSET

    def to_dict(self) -> dict[str, Any]:
        addresses: list[dict[str, Any]] | Unset = UNSET
        if not isinstance(self.addresses, Unset):
            addresses = []
            for addresses_item_data in self.addresses:
                addresses_item = addresses_item_data.to_dict()
                addresses.append(addresses_item)

        field_dict: dict[str, Any] = {}

        field_dict.update({})
        if addresses is not UNSET:
            field_dict["addresses"] = addresses

        return field_dict

    @classmethod
    def from_dict(cls: type[T], src_dict: Mapping[str, Any]) -> T:
        from ..models.on_chain_payment_method_preview_result_address_item import (
            OnChainPaymentMethodPreviewResultAddressItem,
        )

        d = dict(src_dict)
        _addresses = d.pop("addresses", UNSET)
        addresses: list[OnChainPaymentMethodPreviewResultAddressItem] | Unset = UNSET
        if _addresses is not UNSET:
            addresses = []
            for addresses_item_data in _addresses:
                addresses_item = OnChainPaymentMethodPreviewResultAddressItem.from_dict(addresses_item_data)

                addresses.append(addresses_item)

        on_chain_payment_method_preview_result_data = cls(
            addresses=addresses,
        )

        return on_chain_payment_method_preview_result_data
