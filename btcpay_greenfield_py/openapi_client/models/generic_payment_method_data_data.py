# coding: utf-8

"""
    BTCPay Greenfield API

    A full API to use your BTCPay Server

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
from inspect import getfullargspec
import json
import pprint
import re  # noqa: F401

from typing import Any, Dict, List, Optional
from pydantic import BaseModel, Field, StrictStr, ValidationError, field_validator
from pydantic import Field
from openapi_client.models.lightning_network_payment_method_base_data import LightningNetworkPaymentMethodBaseData
from openapi_client.models.on_chain_payment_method_base_data import OnChainPaymentMethodBaseData
from typing import Union, Any, List, TYPE_CHECKING, Optional, Dict
from typing_extensions import Literal
from pydantic import StrictStr, Field
try:
    from typing import Self
except ImportError:
    from typing_extensions import Self

GENERICPAYMENTMETHODDATADATA_ONE_OF_SCHEMAS = ["Dict[str, object]", "LightningNetworkPaymentMethodBaseData", "OnChainPaymentMethodBaseData"]

class GenericPaymentMethodDataData(BaseModel):
    """
    Associated dynamic data based on payment method type.
    """
    # data type: LightningNetworkPaymentMethodBaseData
    oneof_schema_1_validator: Optional[LightningNetworkPaymentMethodBaseData] = None
    # data type: OnChainPaymentMethodBaseData
    oneof_schema_2_validator: Optional[OnChainPaymentMethodBaseData] = None
    # data type: Dict[str, object]
    oneof_schema_3_validator: Optional[Dict[str, Any]] = Field(default=None, description="Any other unofficial payment method data")
    actual_instance: Optional[Union[Dict[str, object], LightningNetworkPaymentMethodBaseData, OnChainPaymentMethodBaseData]] = None
    one_of_schemas: List[str] = Literal["Dict[str, object]", "LightningNetworkPaymentMethodBaseData", "OnChainPaymentMethodBaseData"]

    model_config = {
        "validate_assignment": True
    }


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = GenericPaymentMethodDataData.model_construct()
        error_messages = []
        match = 0
        # validate data type: LightningNetworkPaymentMethodBaseData
        if not isinstance(v, LightningNetworkPaymentMethodBaseData):
            error_messages.append(f"Error! Input type `{type(v)}` is not `LightningNetworkPaymentMethodBaseData`")
        else:
            match += 1
        # validate data type: OnChainPaymentMethodBaseData
        if not isinstance(v, OnChainPaymentMethodBaseData):
            error_messages.append(f"Error! Input type `{type(v)}` is not `OnChainPaymentMethodBaseData`")
        else:
            match += 1
        # validate data type: Dict[str, object]
        try:
            instance.oneof_schema_3_validator = v
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in GenericPaymentMethodDataData with oneOf schemas: Dict[str, object], LightningNetworkPaymentMethodBaseData, OnChainPaymentMethodBaseData. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in GenericPaymentMethodDataData with oneOf schemas: Dict[str, object], LightningNetworkPaymentMethodBaseData, OnChainPaymentMethodBaseData. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: dict) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into LightningNetworkPaymentMethodBaseData
        try:
            instance.actual_instance = LightningNetworkPaymentMethodBaseData.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into OnChainPaymentMethodBaseData
        try:
            instance.actual_instance = OnChainPaymentMethodBaseData.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into Dict[str, object]
        try:
            # validation
            instance.oneof_schema_3_validator = json.loads(json_str)
            # assign value to actual_instance
            instance.actual_instance = instance.oneof_schema_3_validator
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into GenericPaymentMethodDataData with oneOf schemas: Dict[str, object], LightningNetworkPaymentMethodBaseData, OnChainPaymentMethodBaseData. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into GenericPaymentMethodDataData with oneOf schemas: Dict[str, object], LightningNetworkPaymentMethodBaseData, OnChainPaymentMethodBaseData. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        to_json = getattr(self.actual_instance, "to_json", None)
        if callable(to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Dict:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        to_dict = getattr(self.actual_instance, "to_dict", None)
        if callable(to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


