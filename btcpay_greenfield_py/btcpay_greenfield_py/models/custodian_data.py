# coding: utf-8

"""
    BTCPay Greenfield API

    A full API to use your BTCPay Server

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json


from typing import Any, ClassVar, Dict, List, Optional
from pydantic import BaseModel, StrictStr
from pydantic import Field
from btcpay_greenfield_py.models.asset_pair_data import AssetPairData
try:
    from typing import Self
except ImportError:
    from typing_extensions import Self

class CustodianData(BaseModel):
    """
    CustodianData
    """ # noqa: E501
    code: Optional[StrictStr] = Field(default=None, description="The unique code of the custodian.")
    label: Optional[StrictStr] = Field(default=None, description="The name of the custodian.")
    depositable_payment_methods: Optional[List[StrictStr]] = Field(default=None, description="A list of payment methods (crypto code + network) you can deposit to the custodian.", alias="depositablePaymentMethods")
    withdrawable_payment_methods: Optional[List[StrictStr]] = Field(default=None, description="A list of payment methods (crypto code + network) you can withdraw from the custodian.", alias="withdrawablePaymentMethods")
    tradable_asset_pairs: Optional[List[AssetPairData]] = Field(default=None, description="A list of tradable asset pair objects, or NULL if the custodian cannot trades/convert assets.", alias="tradableAssetPairs")
    __properties: ClassVar[List[str]] = ["code", "label", "depositablePaymentMethods", "withdrawablePaymentMethods", "tradableAssetPairs"]

    model_config = {
        "populate_by_name": True,
        "validate_assignment": True
    }


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Create an instance of CustodianData from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        _dict = self.model_dump(
            by_alias=True,
            exclude={
            },
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in tradable_asset_pairs (list)
        _items = []
        if self.tradable_asset_pairs:
            for _item in self.tradable_asset_pairs:
                if _item:
                    _items.append(_item.to_dict())
            _dict['tradableAssetPairs'] = _items
        # set to None if tradable_asset_pairs (nullable) is None
        # and model_fields_set contains the field
        if self.tradable_asset_pairs is None and "tradable_asset_pairs" in self.model_fields_set:
            _dict['tradableAssetPairs'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Dict) -> Self:
        """Create an instance of CustodianData from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "code": obj.get("code"),
            "label": obj.get("label"),
            "depositablePaymentMethods": obj.get("depositablePaymentMethods"),
            "withdrawablePaymentMethods": obj.get("withdrawablePaymentMethods"),
            "tradableAssetPairs": [AssetPairData.from_dict(_item) for _item in obj.get("tradableAssetPairs")] if obj.get("tradableAssetPairs") is not None else None
        })
        return _obj


